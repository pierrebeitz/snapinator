import '../src/reset.css';

/** @type {import('@storybook/react-vite').Preview} */
export default {
  parameters: {
    // A snapshot of a centred component is mostly padding. Fill the frame.
    layout: 'fullscreen',
  },
};
